> 感觉网卡的知识，既算计算机组成原理，又算计算机网络。毕竟技术本就是组合在一起才有了更强大的能力。
>
> 但我选择将文章放在网络主题下。也许会复制一份到计算机组成原理主题。

# 关于网卡

网卡工作在OSI模型的物理层和数据链路层。

> 或者说网卡是运行在数据链路层的，但他的工作又是面向物理层的。
>
> 即网卡在物理层面实现数据链路层的通信。遵循着数据链路层的协议，完成着物理层的工作。

数据链路层有明确的协议，即mac物理地址协议。



网卡有许多种：

按照数据链路层控制来分有以太网卡、令牌环网卡、ATM网卡等。

按照物理层来分有无线网卡、RJ-45网卡、同轴电缆网卡、光纤网卡等。

网卡需要兼顾物理层（连接方式、数据的编码、信号传输的戒指、电平）和数据链路层（控制、寻址、帧结构）



以以太网卡为例

# 网卡的组成

一块以太网卡包括



# Linux网卡配置
CentOS

```bash
vi /etc/sysconfig/network-scriptes/eth_name
```



Ubuntu

```bash
vi /etc/network/interfaces
```

